# --- colcon build artifacts (regenerated by `colcon build`, never committed) ---
build/
install/
log/

# --- recorded rosbags: large binaries, published via LFS / external link ---
bags/

# --- Python / pytest caches ---
__pycache__/
*.pyc
.pytest_cache/

# --- editor / OS cruft ---
.vscode/
.idea/
.DS_Store
.history/

# --- stray run logs / graphviz dumps that sometimes land in the tree ---
*.log
frames_*.gv
frames_*.pdf

# --- internal planning doc: candid audit/owner notes, not for the public repo ---
# (delete this line if you decide to publish the roadmap as portfolio material)
PORTFOLIO_ROADMAP.md

# NOTE: scripts/ is intentionally TRACKED (owner decision: show figure-gen code).
# NOTE: ARCHITECTURE.md, SPRINTS.md, drone_slam_stories/, and vendored px4_msgs/
#       live at src/ (OUTSIDE this repo root) and are excluded automatically —
#       no entry needed here.

# ---- OWNER ADD HERE ----
# (list any extra files/folders you want excluded before finalizing)
